# Advanced features — each in its own file, imported and wired by conftest.py.
# replay.py      → animated GIF of last actions before a failure
# self_heal.py   → one-fallback auto-repair for drifted selectors
# flaky.py       → cross-run stability detection via JSONL history
