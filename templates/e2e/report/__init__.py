# Report customisation — JS dashboard, CSS theme, pytest-html hook helpers.
# hooks.py          → column injection + JS/CSS injection into pytest-html
# report_theme.js   → full custom dashboard (replaces pytest-html's raw table)
# report-style.css  → dark theme for .tee-* elements
